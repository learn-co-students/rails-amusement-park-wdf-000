<%= @attraction.name %>

<%= button_to 'Go on this ride', user_path(current_user), method: :patch, params: {attraction_id: @attraction.id } unless current_user.admin %>
<br />

<%= link_to 'Edit Attraction', edit_attraction_path(@attraction) if current_user.admin %>

