<%= @attraction.name %>
<%= %>
<%= %>
<%= %>
<%= %>
<%= %>
<%= (link_to "Edit Attraction", edit_attraction_path(@attraction)) if @current_user.admin? %>
<%= (link_to "Delete", attractions_path, method: :delete) if @current_user.admin? %>
<% if !@current_user.admin? %>
<form action = "/rides" method = "post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :attraction_id, @attraction.id %>
  <%= hidden_field_tag :user_id, @current_user.id %>
  <input type="submit" value="Go on this ride">
</form>
<% end %>
